#include<stdio.h>
int main()
{
    int i,j,n;
    scanf("%d",&n);
    for(int i=1;i<=n;i++,printf("\n"))  
    {
        for(int j=1;j<=n;j++)
        if(i<=j)
        printf("%d ",j);
        else
        printf("  ");
    }
    return 0;
}
